plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.neural_nexus2"
    compileSdk = 34
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = '1.8'  // Simplified to string literal
        targetCompatibility = '1.8'  // Simplified to string literal
    }

    kotlinOptions {
        jvmTarget = '1.8'  // Simplified to string literal
    }

    defaultConfig {
        applicationId = "com.example.neural_nexus2"
        minSdk = 16
        targetSdk = 34
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}